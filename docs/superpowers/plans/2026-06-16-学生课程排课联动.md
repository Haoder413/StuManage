# 学生课程排课联动 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增学生时选择课程和固定时间，保存后自动在排课日历显示。

**Architecture:** 前端新增学生页加载课程列表并提交课程与时间字段。后端学生创建接口在同一请求中创建学生、学生课程关联和固定排课。

**Tech Stack:** Next.js App Router, React, Prisma, SQLite.

---

### Task 1: Add Regression Checks

**Files:**
- Create: `scripts/check-student-course-schedule-link.mjs`

- [ ] Check student page contains course/time fields.
- [ ] Check student API creates `studentCourses` and `schedules`.

### Task 2: Update Student Create API

**Files:**
- Modify: `src/app/api/students/route.ts`

- [ ] Include `studentCourses` and `schedules` in student list data.
- [ ] On POST, create optional `studentCourses` and optional fixed `schedules`.

### Task 3: Update New Student Form

**Files:**
- Modify: `src/app/students/new/page.tsx`

- [ ] Load `/api/courses`.
- [ ] Add course selector and fixed schedule fields.
- [ ] Submit `courseId`, `dayOfWeek`, `startTime`, and `endTime`.

### Task 4: Verify

- [ ] Run `node scripts/check-student-course-schedule-link.mjs`.
- [ ] Run existing regression checks.
- [ ] Run `npm run build`.

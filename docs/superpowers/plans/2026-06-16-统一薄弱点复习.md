# 统一薄弱点复习 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 合并薄弱点、复习计划、历史薄弱点为一个可筛选、可直接操作的薄弱点复习列表。

**Architecture:** 继续复用现有 active/history 数据加载和 weak-points PATCH 接口。前端在学生学习进度详情页合并数据、计算状态和筛选结果。

**Tech Stack:** Next.js App Router, React, Prisma。

---

### Task 1: Regression Check

- [ ] Update `scripts/check-weak-point-mastered.mjs` to require the unified weak-point review UI.

### Task 2: Student Progress Page

- [ ] Modify `src/app/progress/students/[id]/page.tsx` to remove separate review/history tabs.
- [ ] Add filter state and unified weak-point cards.
- [ ] Keep actions available in the same list.

### Task 3: Verify

- [ ] Run weak-point regression check.
- [ ] Run existing regression checks.
- [ ] Run `npm run build`.
